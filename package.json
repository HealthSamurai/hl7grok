{
  "name": "hl7grok",
  "version": "0.0.1",
  "dependencies": {
  },
  "devDependencies": {
    "coffee-script": "latest",
    "mocha": "latest",
    "js-yaml": "latest"
  },
  "engines": {
    "node": ">=0.12.0"
  },
  "scripts": {
    "build": "`npm bin`/coffee -c -o lib/ src/*.coffee",
    "watch": "`npm bin`/coffee -c -w -o lib/ src/*.coffee"
  },
  "preferGlobal": false,
  "bin": {},
  "scripts": {
    "build": "`npm bin`/coffee -c -o lib/ src/*.coffee",
    "watch": "`npm bin`/coffee -c -w -o lib/ src/*.coffee",
    "test": "`npm bin`/mocha --compilers coffee:coffee-script/register test/runner.coffee || echo 'Tests failed'"
  }
}
